DEBUG=True
SECRET_KEY=replace-with-a-long-random-secret
ALLOWED_HOSTS=127.0.0.1,localhost

# Optional: if omitted, the app falls back to local SQLite.
# Example PostgreSQL URL:
# DATABASE_URL=postgres://team322:change-me@localhost:5432/habittracker
